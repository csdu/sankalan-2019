html
  head
    include _head
  body(class={"is-front-page": page.slug === ''})
    include _header
    .loader
      p &gt; loading...#[br]
      include _loader-svg.html
    #content.site-content
      include _splash
      nav.nav
        .nav-container
          include _nav
          +Nav('splash')
      .content-wrapper
        #main.main-content !{content}
    .pattern-background
    canvas#canvas.bg-scene
    include _footer
